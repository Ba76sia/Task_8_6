var a = 3;
var b = 4;
var value = (a * a) + (2 * a * b) - (b * b);
console.log(value);
var positiveOrNegative = value > 0 ? 'variable value is positive' : 'variable value is negative';
console.log(positiveOrNegative);
var equalsNull = value = 0 ? "variable value equals 0" : "variable value doesn't equal 0";
console.log(equalsNull);